//Valores falsos
Boolean(0); //Cero siempre sera falso
Boolean(null)
Boolean(NaN);
Boolean(undefined);
Boolean(false);
Boolean("");

//Valores Verdaderos
Boolean(1); //Uno en adelante es verdadero
Boolean("a"); //Cualquier caracter en string es verdadero
Boolean([]); //Cualquier array ya sea vacio sera verdadero
Boolean({}); //Cualquier objeto aun que este vacio sera verdadero
Boolean(function(){}); //Cualquier funcion sera verdadera 